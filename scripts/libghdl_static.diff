diff --git a/Makefile.in b/Makefile.in
index 50e63004..cecbd8cd 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -405,6 +405,8 @@ uninstall.simul: uninstall.simul.program uninstall.vhdllib
 LIBGHDL_INCFLAGS=$(GHDL_COMMON_INCFLAGS) -aI$(srcdir)/src/synth -aI$(srcdir)/src/vhdl/libghdl -aI$(srcdir)/src/ghdldrv
 
 libghdl_name=libghdl-$(libghdl_version)$(SOEXT)
+libghdl_static_name=libghdl.a
+libghdl_static_linkfile=ghdl.link
 
 LIBGHDL_GRT_OBJS= pic/grt-cstdio.o
 
@@ -417,6 +419,15 @@ ifeq ($(SOEXT),.dll)
 	-mv $(libghdl_name).exe $(libghdl_name)
 endif
 
+$(libghdl_static_name): $(GRT_SRC_DEPS) $(LIBGHDL_GRT_OBJS) version.ads force
+	$(GNATMAKE) -c libghdl $(GNATFLAGS) -gnat12 $(LIBGHDL_INCFLAGS)
+	gnatbind -Llibghdl_ libghdl.ali -O > ghdl.files
+#	Extract link options, be sure to link statically with libgnat
+	gnatbind -Llibghdl_ libghdl.ali -K -Z | sed -e '\@adalib/$$@s/-L//' -e '\@adalib/@s@adalib/@adalib/libgnat.a@' -e '/-lgnat/d' > $(libghdl_static_linkfile)
+	$(GNATMAKE) -c b~libghdl.adb
+	$(RM) -f $@
+	ar rc $@ b~libghdl.o `cat ghdl.files` $(LIBGHDL_GRT_OBJS)
+
 $(srcdir)/src/synth/ghdlsynth_gates.h: $(srcdir)/src/synth/netlists.ads $(srcdir)/src/synth/netlists-gates.ads
 	echo "/* DO NOT MODIFY" > $@
 	echo "   This file is automatically generated by Makefile.  */" >> $@
@@ -428,7 +439,7 @@ $(srcdir)/src/synth/ghdlsynth_gates.h: $(srcdir)/src/synth/netlists.ads $(srcdir
 	grep '      Param_' $(srcdir)/src/synth/netlists.ads >> $@
 	echo "};" >> $@
 
-all.libghdl.true: $(libghdl_name)
+all.libghdl.true: $(libghdl_name) $(libghdl_static_name)
 all.libghdl.false:
 all.libghdl: all.libghdl.$(enable_libghdl)
 
@@ -438,6 +449,8 @@ install.libghdl.include: install.dirs $(srcdir)/src/synth/ghdlsynth_gates.h
 
 install.libghdl.lib:
 	$(INSTALL_PROGRAM) -p $(libghdl_name) $(DESTDIR)$(libdir)/
+	$(INSTALL_PROGRAM) -p $(libghdl_static_name) $(DESTDIR)$(libdir)/
+	$(INSTALL_PROGRAM) -p $(libghdl_static_linkfile) $(DESTDIR)$(libdir)/
 
 install.libghdl.true: install.libghdl.lib install.libghdl.include
 
@@ -446,6 +459,8 @@ install.libghdl: install.libghdl.$(enable_libghdl)
 
 uninstall.libghdl:
 	$(RM) $(DESTDIR)$(libdir)/$(libghdl_name)
+	$(RM) $(DESTDIR)$(libdir)/$(libghdl_static_name)
+	$(RM) $(DESTDIR)$(libdir)/$(libghdl_static_linkfile)
 
 libghdl-py.tgz:
 	[ -d pythonb ] || $(MKDIR) pythonb
