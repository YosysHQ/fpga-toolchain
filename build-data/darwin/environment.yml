name: base
channels:
  - defaults
  - conda-forge
  - litex-hub # for libftdi
dependencies:
  - python=3.7
  - bison
  - eigen
  - flex
  - gawk
  - gettext
  - conda-forge::boost # includes py-boost, has more recent releases
  - libftdi
  - libiconv
  - libusb
  - m4
  - make
  - ncurses
  - readline=8.0